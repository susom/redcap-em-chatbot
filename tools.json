{
  "_note": "Cappy's own agent tools. These are FRONTEND/UI actions executed client-side by assets/cappy-actions.js (the in-page renderer). The server handlers in REDCapChatBot::redcap_module_api just echo intent so the SecureChatAI agent loop can continue; the actual highlight/fill happens in the browser from the tools_used arguments. To activate, add this module's prefix (redcap-em-chatbot) to SecureChatAI's project_agent_tool_em_prefixes (or the system-level agent_tool_em_prefixes).",
  "tools": [
    {
      "name": "page.highlight",
      "description": "Highlight (draw a gold ring around) a field or element on the REDCap page the user is CURRENTLY viewing, to show them where to click or look. Use this whenever the user asks you to point to, show, highlight, or find something on the page. PREFERRED: pass `text` (a short descriptive phrase naming the control — e.g. 'Twilio SMS and Voice Call services'). The browser will deterministically keyword-match against the live page, so you don't have to pick the right control_id from a list. FALLBACKS: control_id (if you already know it) or field (only for actual REDCap data-entry fields).",
      "action": "page_highlight",
      "parameters": {
        "type": "object",
        "properties": {
          "text": { "type": "string", "description": "PREFERRED. A short descriptive phrase naming what to highlight (e.g. 'Twilio SMS', 'MyCap', 'SendGrid', 'Save button'). Browser keyword-matches against the live page." },
          "field": { "type": "string", "description": "FALLBACK. REDCap variable/field name, only for data-entry fields." },
          "control_id": { "type": "string", "description": "FALLBACK. control_id from the 'Page elements' list, only if you're sure." },
          "label": { "type": "string", "description": "Optional human-friendly label to show on the highlight tooltip." }
        }
      },
      "readOnly": true,
      "destructive": false
    },
    {
      "name": "page.fill",
      "description": "Propose a value for a data-entry field on the REDCap page the user is currently viewing. The user is shown a confirmation prompt before the value is written to the page. Provide the REDCap field/variable name and the value.",
      "action": "page_fill",
      "parameters": {
        "type": "object",
        "properties": {
          "field": { "type": "string", "description": "REDCap variable/field name to fill" },
          "value": { "type": "string", "description": "Value to propose for the field" },
          "label": { "type": "string", "description": "Optional human-friendly label for the confirmation prompt" }
        },
        "required": ["field", "value"]
      },
      "readOnly": false,
      "destructive": false
    },
    {
      "name": "page.clearHighlights",
      "description": "Remove any highlight rings currently drawn on the REDCap page the user is viewing.",
      "action": "page_clearHighlights",
      "parameters": { "type": "object", "properties": {} },
      "readOnly": true,
      "destructive": false
    }
  ]
}
